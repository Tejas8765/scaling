#include <iostream.h>
#include <conio.h>
int main()
{
    int a,remaind;
    clrscr();
    cout<<"enter a no.";
    cin>>a;

    if((a%2)==0)
    {
        cout<<"the entered no. is even no.";
    }
    else
    {
        cout<<"entered no. is odd no. ";
    }
    getch()
    return 0;
}
